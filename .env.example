# Use either a connection string or the standard PG* variables below.
DATABASE_URL=postgresql://postgres:replace-me@localhost:5432/course_registration
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Used only to reproduce the deliberately unsafe race-condition demo.
# Leave false for all normal use.
UNSAFE_NO_LOCK=false
